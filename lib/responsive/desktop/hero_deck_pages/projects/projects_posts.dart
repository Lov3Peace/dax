import 'package:flutter/material.dart';
import 'package:flutter_application_1/util/decks.dart';
import '../../../../util/imports.dart';

class ProjectCategory extends StatelessWidget {
  const ProjectCategory({super.key, required this.title, required this.content, required this.user, required this.timestamp});
  final String title;
  final String user;
  final String content;
  final String timestamp;

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
